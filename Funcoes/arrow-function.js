// Arrow Fuction

// Declaração tradicional

/* function soma (x, y) {
    const resultado = x + y;
    return resultado;
} */

// Declaração com arrow-function
const soma = (x, y) => {
    const resultado = x + y;
    return resultado;
}